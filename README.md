# Python.HW1
Первая ДЗ по Пайтону

# # Задача 1. Определяет является ли день недели выходным

# import os

# os.system('cls')

# while True:
#     try: 
#         input_int = int(input('Введите число от 1 до 7: \n'))
#         if 1 <= input_int <= 7: break
#         else: 
#             print('Число меньше 1 либо больше 7!\n')
#             continue
#     except: 
#         print('Не верно введено число!\n')
#         continue

# day_of_week = {
#     1: '1 день недели НЕ является выходным!',
#     2: '2 день недели НЕ является выходным!',
#     3: '3 день недели НЕ является выходным!',
#     4: '4 день недели НЕ является выходным!',
#     5: '5 день недели НЕ является выходным! Но это уже ПЯТНИЦА!',
#     6: '6 день недели ЯВЛЯЕТСЯ выходным и это СУББОТА!',
#     7: '7 день недели ЯВЛЯЕТСЯ выходным и это ВОСКРЕСЕНЬЕ!'
# }

# print(day_of_week[input_int])

# ********************************************************************************************************************************

# Задача 2. Программа для проверки истинности утверждения ¬(X ⋁ Y ⋁ Z) = ¬X ⋀ ¬Y ⋀ ¬Z для всех значений предикат.

# import os

# os.system('cls')

# for X in range(2):
#     for Y in range(2):
#         for Z in range (2):
#             print('\nдля X = ', X , ' Y = ', Y, ' и Z = ', Z, ' выражение ¬(X V Y V Z) = ¬X ⋀ ¬Y ⋀ ¬Z -> ')
#             print(not(X or Y or Z) == (not X and not Y and not Z))

# ********************************************************************************************************************************

# Задача 3. Программа, которая принимает на вход координаты точки (X и Y), причём X ≠ 0 и Y ≠ 0 и выдаёт номер четверти плоскости, 
# в которой находится эта точка (или на какой оси она находится).

# import os

# os.system('cls')

# count = 1

# while True:
#     try: 
#         if count == 1: 
#             input_x = int(input('Введите целое число координаты Х: \n'))
#             count = 2
#         else: 
#             input_y = int(input('Введите целое число координаты Y: \n'))
#             break
#     except: 
#         print('Не верно указана координата!\n')

# if input_x == 0 and input_y == 0: str = 'Точка находится в самом центри оси координат!'
# elif input_x == 0: str = 'Точка лежит на оси X'
# elif input_y == 0: str = 'Точка лежит на оси Y'
# elif input_x > 0 and input_y > 0: str = 'Точка лежит в I четверти оси координат!'
# elif input_x < 0 and input_y > 0: str = 'Точка лежит в II четверти оси координат!'
# elif input_x < 0 and input_y < 0: str = 'Точка лежит в III четверти оси координат!'
# elif input_x > 0 and input_y < 0: str = 'Точка лежит в IV четверти оси координат!'

# print('\n' + str)

# ********************************************************************************************************************************

# Задача 4. Программа, которая по заданному номеру четверти, показывает диапазон возможных координат точек в этой четверти (x и y).

# import os

# os.system('cls')

# while True:
#     try: 
#         input_int = int(input('Введите номер четверти от 1 до 4: \n'))
#         if 1 <= input_int <= 4: break
#         else: 
#             print('Вы ввели число меньше 1 либо больше 4!\n')
#             continue
#     except: 
#         print('Не верно указан номер четверти!\n')
#         continue

# num_of_quater = {
#     1: 'в первой четверти х > 0 и y > 0',
#     2: 'во второй четверти х < 0, а y > 0',
#     3: 'в третьей четверти х < 0 и y < 0',
#     4: 'в четвёртой четверти х > 0, а y < 0',
# }

# print(num_of_quater[input_int])

# ********************************************************************************************************************************

# Задача 5. Программа, которая принимает на вход координаты двух точек и находит расстояние между ними в 2D пространстве.

# import os
# import math

# os.system('cls')

# count = 1

# while True:
#     try: 
#         if count == 1: 
#             input_x_first = int(input('Введите целое число ПЕРВОЙ координаты ПЕРВОЙ точки: \n'))
#             count = 2
#         elif count == 2:
#             input_y_first = int(input('Введите целое число ВТОРОЙ координаты ПЕРВОЙ точки: \n'))
#             count = 3
#         elif count == 3:
#             input_x_second = int(input('Введите целое число ПЕРВОЙ координаты ВТОРОЙ точки: \n'))
#             count = 4
#         else: 
#             input_y_second = int(input('Введите целое число ВТОРОЙ координаты ВТОРОЙ точки: \n'))
#             break
#     except: 
#         print('Не верно указана координата!\n')

# print('\nРасстояние между указанными точками = ')
# print(math.sqrt(pow((input_x_second - input_x_first), 2) + pow((input_y_second - input_y_first), 2)))
